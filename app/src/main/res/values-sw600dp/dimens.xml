<resources>
    <!-- Example customization of dimensions originally defined in res/values/dimens.xml
         (such as screen margins) for screens with more than 820dp of available width. This
         would include 7" and 10" devices in landscape (~960dp and ~1280dp respectively). -->
    <dimen name="activity_horizontal_margin">0dp</dimen>
    <dimen name="activity_vertical_margin">0dp</dimen>

    <!-- Discover -->
    <dimen name="activity_discover_grid_height">240dp</dimen>

    <!-- Discover: Detail Movie -->
    <dimen name="activity_discover_detail_movie_title_box_height">72dp</dimen>
    <dimen name="activity_discover_detail_movie_poster_width">128dp</dimen>
    <dimen name="activity_discover_detail_movie_poster_height">164dp</dimen>

</resources>
